import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Nomi Cose Città',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
